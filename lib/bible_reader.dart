
import 'package:flutter/material.dart';

class BibleReader extends StatelessWidget {
  const BibleReader({super.key});


  @override
  Widget build(BuildContext context) {
    return Text("this will be a bible reader");
  }
}